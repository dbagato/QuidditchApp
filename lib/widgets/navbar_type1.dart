import 'package:flutter/material.dart';

class NavBarType1 extends StatelessWidget {
  const NavBarType1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO navbar generico pra los usuarios, implementar posible menu lateral
    return AppBar();
  }
}
